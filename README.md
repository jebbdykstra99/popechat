# popechat

Urbi et orbi. To the city and the world.

Static SubX dress rehearsal for popechat.com. Dummy feed about the papacy and the Holy See. Not official Vatican. Not a meme. Not X.com.

## Pages
Deploy `main` from `/` (root). Custom domain `popechat.com`. Leave HTTPS off until GitHub issues the cert.

## DNS
Four A on `@` to GitHub Pages (`185.199.108.153` `.109.153` `.110.153` `.111.153`). CNAME `www` → `jebbdykstra99.github.io`.
